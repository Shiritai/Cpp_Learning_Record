# include <iostream>
# include <string>
# include <vector>
# include <list>
# include <deque>
# include <bitset>
// # include <>

using namespace std; // Standard Template Library 都在這

int main(void){
    // auto nums = vector<int>(10, -1);
    // auto nums2 = vector<int>(10, -1);
    // for (auto it : nums)
    //     cout << it << " ";
    // cout << endl;
    // int i = 0;
    // for (auto it = nums.begin(); it != nums.end(); ++it)
    //     * it = i++;
    // for (auto it : nums)
    //     cout << it << " ";

    // auto bit = bitset<16 /* size */>();
}